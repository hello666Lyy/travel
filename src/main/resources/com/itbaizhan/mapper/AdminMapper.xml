<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itbaizhan.mapper.AdminMapper">

    <resultMap id="adminMap" type="com.itbaizhan.pojo.Admin">
        <id property="aid" column="aid"></id>
        <result property="username" column="username"></result>
        <collection property="roles" column="aid" ofType="com.itbaizhan.pojo.Role">
            <result property="roleName" column="roleName"></result>
            <result property="roleDesc" column="roleDesc"></result>
            <collection property="permissions" column="rid" ofType="com.itbaizhan.pojo.Permission">
                <result property="permissionName" column="permissionName"></result>
                <result property="permissionDesc" column="permissionDesc"></result>
            </collection>
        </collection>
    </resultMap>

    <insert id="addAdminRole">
        insert into admin_role values (#{aid}, #{rid})
    </insert>

    <delete id="deleteAdminAllRoles" parameterType="int">
        delete from admin_role where aid = #{aid}
    </delete>

    <select id="findDesc" parameterType="int" resultMap="adminMap">
        select * FROM
            admin
                left join admin_role
                          on admin.aid = admin_role.aid
                left join role
                          on admin_role.rid = role.rid
                left join role_permission
                          on role.rid = role_permission.rid
                left join permission
                          on role_permission.pid = permission.pid
        where admin.aid = #{aid}
    </select>
    <select id="findAllPermission" parameterType="string" resultType="com.itbaizhan.pojo.Permission">
        select distinct permission.* FROM
            admin
                left join admin_role
                          on admin.aid = admin_role.aid
                left join role
                          on admin_role.rid = role.rid
                left join role_permission
                          on role.rid = role_permission.rid
                left join permission
                          on role_permission.pid = permission.pid
        where admin.username = #{username}
          AND permission.pid is not null
    </select>
</mapper>