<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itbaizhan.mapper.RoleMapper">
    <insert id="addRolePermission">
        insert into role_permission values (#{rid},#{pid})
    </insert>
    <delete id="deleteRoleAllPermission" parameterType="int">
        delete from role_permission where rid = #{rid}
    </delete>

    <select id="findRoleldByAdmin" parameterType="int" resultType="java.lang.Integer">
        select rid from admin_role where aid = #{aid}
    </select>

    <resultMap id="roleMap" type="com.itbaizhan.pojo.Role">
        <id property="rid" column="rid"></id>
        <result property="roleName" column="roleName"></result>
        <result property="roleDesc" column="roleDesc"></result>
        <collection property="permissions" column="rid" ofType="com.itbaizhan.pojo.Permission">
            <result property="permissionName" column="permissionName"></result>
            <result property="permissionDesc" column="permissionDesc"></result>
        </collection>
    </resultMap>

    <select id="findDesc" parameterType="int" resultMap="roleMap">
        select * FROM
            role
                left join role_permission
                          on role.rid = role_permission.rid
                left join permission
                          on role_permission.pid = permission.pid
        where role.rid = #{rid}
    </select>
</mapper>